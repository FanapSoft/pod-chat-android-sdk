<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fanap.podchat"
    tools:targetApi="n">

    <application>

        <provider
            android:name=".util.GenericFileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>


        <receiver
            android:name=".notification.ShowNotificationHelper$NotificationClickReceiver">
            <intent-filter>
                <action android:name="com.fanap.podchat.action_click_notification" />
            </intent-filter>
        </receiver>
        <receiver
            android:enabled="true"
            android:name=".notification.ShowNotificationHelper$NotificationDismissReceiver" />

        <service
            android:name="com.fanap.podchat.notification.PodChatPushNotificationService"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>


        <meta-data android:name="io.sentry.dsn" android:value="@string/key"/>

    </application>


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>
